# GlyphMoji-TG

Minimal Telegram bot that supports:
- Emoji cipher (custom chart — matches glyphmoji site)
- Unicode mode (`\uXXXX` style encoding) that can be decoded anywhere
- Per-chat mode switching with `/changemod` (toggle or force)
- `/encode`, `/decode`, `/mode`, `/start`, `/help`
- Health-check `/healthz` for Koyeb
